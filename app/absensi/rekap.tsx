import { View, Text } from "react-native";

export default function AbsensiRekapScreen() {
  return (
    <View style={{ flex: 1, padding: 16 }}>
      <Text style={{ fontSize: 18, fontWeight: "700" }}>Rekap Absensi</Text>
      <Text style={{ opacity: 0.7, marginTop: 8 }}>
        TODO: grafik/rekap harian–mingguan per proyek & pekerja.
      </Text>
    </View>
  );
}
